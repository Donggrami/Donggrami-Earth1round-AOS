<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.earth1round_aos">

    <uses-permission android:name="android.permission.Internet"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Earth1roundAOS"
        tools:targetApi="31"
        android:name=".main.login.GlobalApplication">
<!--        ㄴ로그인이 어플리케이션의 다른 컴포넌트보다 먼저 실행되도록-->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBUWtxZNxa-3dEnA-NoqFnVvox7iL71SkA" />

        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Redirect URI: "kakao${NATIVE_APP_KEY}://oauth" -->
                <data android:host="oauth"
                    android:scheme="kakao7858293610f37e4c9eeb809456609bab" />
            </intent-filter>
        </activity>

        <activity android:name=".main.character.MapsActivity"/>

        <activity
            android:name=".main.MainActivity"/>
        <activity
            android:name=".main.login.LoginActivity"/>
        <activity android:name=".main.signup.SignUpActivity"/>
        <activity android:name=".main.signup.SignUpCharNameActivity"/>
        <activity android:name=".main.signup.SignUpTermsActivity"/>
        <activity android:name=".main.setting.SettingActivity"/>

        <activity
            android:name=".main.splash.SplashActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.donggrami.main.character.CharacterCalenderActivity"
            android:exported="true"/>

    </application>

</manifest>